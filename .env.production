# Baked into `npm run build` automatically by Vite. Safe to commit: this is
# the public API URL only — the ANTHROPIC_API_KEY lives in the backend's
# environment (Render dashboard), never in the frontend.
VITE_API_MODE=live
VITE_API_BASE_URL=https://ecoreceipt-api.onrender.com
